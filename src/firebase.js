// This is a placeholder file to replace the Firebase implementation
// This application now uses localStorage instead of Firebase

// Export mock Firebase objects to prevent import errors
export const auth = null;
export const db = null;
export default null; 